---
import Layout from "@/layouts/Layout.astro";
import PageHeader from "@/components/PageHeader.astro";
import BrandingComparison from "@/components/sections/BrandingComparison.astro";
import BrandingFeatures from "@/components/sections/BrandingFeatures.astro";
import ServicePricing from "@/components/sections/ServicePricing.astro";
import ServiceFAQ from "@/components/sections/ServiceFAQ.astro";
import { useTranslations } from "@/i18n/utils";
import { pricing, PRICES } from "@/config/pricing";

const { t, tRaw } = useTranslations();

type PricingCta = { title: string; description: string; button: string };
type FaqItem = { question: string; answer: string };

const siteUrl = "https://generics.studio";
const faqItems = tRaw<FaqItem[]>("pages.branding.faq.items");

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faqItems.map((item) => ({
    "@type": "Question",
    name: item.question,
    acceptedAnswer: { "@type": "Answer", text: item.answer },
  })),
};

const serviceSchema = {
  "@context": "https://schema.org",
  "@type": "Service",
  name: "Branding & Logodesign",
  serviceType: "Branding",
  description: t("pages.branding.meta.description"),
  provider: { "@id": `${siteUrl}/#organization` },
  areaServed: [
    { "@type": "City", name: "Wil" },
    { "@type": "AdministrativeArea", name: "Kanton St. Gallen" },
    { "@type": "Country", name: "Schweiz" },
  ],
  offers: { "@type": "AggregateOffer", lowPrice: String(PRICES.branding.logo), priceCurrency: "CHF", offerCount: "3" },
};

const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    { "@type": "ListItem", position: 1, name: "Home", item: `${siteUrl}/` },
    { "@type": "ListItem", position: 2, name: "Branding", item: `${siteUrl}/branding` },
  ],
};
---

<Layout
  title={t("pages.branding.meta.title")}
  description={t("pages.branding.meta.description")}
  schema={[serviceSchema, breadcrumbSchema, faqSchema]}
>
  <main>
    <PageHeader
      title={t("pages.branding.header.title")}
      description={t("pages.branding.header.description")}
    />
    <BrandingComparison locale="de" />
    <BrandingFeatures locale="de" />
    <ServicePricing
      title={t("pages.branding.pricing.title")}
      description={t("pages.branding.pricing.description")}
      note={t("pages.branding.pricing.note")}
      cta={tRaw<PricingCta>("pages.branding.pricing.cta")}
      plans={pricing.branding.plans}
    />
    <ServiceFAQ
      title={t("pages.branding.faq.title")}
      items={faqItems}
    />
  </main>
</Layout>
